<% if @domain.errors.any? %>
<div id="error_explanation">
  <div class="alert alert-danger">
		<%= pluralize(@domain.errors.count, "error") %> prohibited this page from being saved:
	</div>
  <ul>
  <% @domain.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
<div class="form-group">
	<%= f.label :name %>
	<%= f.text_field :name, :class => "form-control" %>
</div>
<div class="form-group">
	<%= f.label :site_id %>
	<%= f.select :site_id, Site.order('name asc').collect { |c| [c.name, c.id] }, {}, :class => "form-control" %>
</div>

