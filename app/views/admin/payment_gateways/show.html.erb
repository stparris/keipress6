<div class="row">
	<div class="col-md-9">
		<div class="page-header">
			<h1><%= @payment_gateway.name %></h1>
		</div>	
		<%= object_table(@payment_gateway) %>
		<%= link_to edit_admin_payment_gateway_path(@payment_gateway) do %>
			<button type="submit" class="btn  btn-outline-secondary"><i class="icon-pencil"></i> Edit</button>
		<% end %>
		&nbsp;&nbsp;<%= link_to admin_payment_gateway_path(@payment_gateway), method: :delete, data: { confirm: "Delete payment_gateway?" } do %>
			<button type="submit" class="btn btn-danger"><i class="icon-trash"></i> Delete</button>
		<% end %>
	</div>
	<div class="col-md-3">
		<%= operations_menu(@payment_gateway) %>
	</div>
</div>
