<% if @container.errors.any? %>
<div id="error_explanation">
  <div class="alert alert-danger">
		<%= pluralize(@container.errors.count, "error") %> prohibited this page from being saved:
	</div>
  <ul>
  <% @container.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
<%= f.hidden_field :site_id, value: @site.id %>
<%= f.hidden_field :container_type %>
<div class="form-group">
	<%= f.label :name %>
	<%= f.text_field :name, :class => "form-control" %>
</div>

<%= render partial: "form_#{@container.container_type}", locals: {f:f} %>

