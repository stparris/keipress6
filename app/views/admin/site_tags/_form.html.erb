<%= form_with(model: site_tag, local: true) do |form| %>
  <% if site_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site_tag.errors.count, "error") %> prohibited this site_tag from being saved:</h2>

      <ul>
        <% site_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
