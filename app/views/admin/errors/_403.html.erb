<div class="row">
	<div class="col-md-12">
		<div class="page-header">
			<h1>Forbidden, Error 403</h1>
		</div>	
		<p>You do not have permission to view the requested resource.</p>
		<%= raw("<p>#{@error.message}</p>") if @error.message %>
		<p>Return to the site's home page<%= link_to ' by clicking here: Return Home.', admin_site_url(@site.id) if @site %>.</p>
	</div>
</div>