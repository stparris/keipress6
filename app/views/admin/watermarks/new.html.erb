<div class="row">
	<div class="col-md-9">
		<div class="page-headerr">
		  <h1>New Watermark</h1>
		</div>
		<%= form_for([:admin, @watermark],:html => {:role => 'form', :class => "formFor"}) do |f| %>
			<%= render partial: 'form', locals: {f:f} %>
			<hr />
			<%= button_tag(type: 'submit', class:"btn  btn-outline-secondary btn-med") do %>
				<icon class="icon-ok"></icon> Create
			<% end %>
			<%= link_to(raw('<button type="button" class="btn  btn-outline-secondary btn-med btn-right">
				<i class="icon-cancel"></i>
		   	 	Cancel</button>'), admin_watermarks_path) %>
		<% end %>
	</div>
	<div class="col-md-3">
		<%= operations_menu(@watermark,[]) %>
	</div>
</div>

