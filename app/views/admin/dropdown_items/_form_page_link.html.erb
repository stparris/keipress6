<div class="bs-help">
	<p>Use Bootstrap CSS classes plus your own to set the look and behavior of the dropdown item.</p> 
	<p>Class nav-item is automatically included.</p>
</div>
<label for="container_row_css_classes"><%= f.label :css_classes %></label>
<div class="input-group mb-3">
  <div class="input-group-prepend">
    <span class="input-group-text" id="basic-addon1">nav-item</span>
  </div>
  <%= f.text_field :css_classes, value: @dropdown_item.css_classes ? @dropdown_item.added_css_classes : '', :class => "form-control" %>
</div>

<div class="form-bar">
	<%= f.label :page_id %>
	<%= f.select :page_id, Page.where("site_id = ? and id not in (select page_id from dropdown_items where dropdown_id = ?)", @site.id, @dropdown_item.dropdown.id).order('name asc').collect { |c| [c.name, c.id] }, {prompt: 'Please Select'}, :class => "form-control" %>
</div>