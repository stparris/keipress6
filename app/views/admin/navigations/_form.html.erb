<% if @navigation.errors.any? %>
<div id="error_explanation">
  <div class="alert alert-danger">
		<%= pluralize(@navigation.errors.count, "error") %> prohibited this page from being saved:
	</div>
  <ul>
  <% @navigation.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
<%= f.hidden_field :site_id, value: @site.id %>
<%= f.hidden_field :nav_type %>
<div class="form-bar">
	<%= f.label :name %>
	<%= f.text_field :name, :class => "form-control" %>
</div>
<%= render partial: "form_#{@navigation.nav_type}", locals: {f:f} %>