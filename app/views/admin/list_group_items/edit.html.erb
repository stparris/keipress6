<div class="row">
	<div class="col-md-9">
		<div class="page-header">
		  <h1>Edit List Group Item</h1>
		</div>
		<%= form_for([:admin, @list_group_item],:html => {:role => 'form', :class => "formFor"}) do |f| %>
			<%= render partial: 'form', locals: {f:f} %>
			<hr />
			<%= button_tag(type: 'submit', class:"btn  btn-outline-secondary btn-med") do %>
				<i class="icon-ok"></i>
				Save
			<% end %>
			<%= link_to(raw('<button type="button" class="btn  btn-outline-secondary btn-med btn-right">
				<i class="icon-cancel"></i>
		   	 	Cancel</button>'), admin_list_group_item_path(@list_group_item)) %>
		<% end %>
	</div>
	<div class="col-md-3">
		<%= operations_menu(@list_group_item,[@list_group]) %>
	</div>
</div>
