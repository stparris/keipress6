<div class="bs-help">
	<p>Use Bootstrap CSS classes plus your own to set the look and behavior of the list_group item.</p> 
	<p>Class list-group-item is automatically included.</p>
</div>
<label for="container_row_css_classes"><%= f.label :css_classes %></label>
<div class="input-group mb-3">
  <div class="input-group-prepend">
    <span class="input-group-text" id="basic-addon1">list-group-item</span>
  </div>
  <%= f.text_field :css_classes, value: @list_group_item.css_classes ? @list_group_item.added_css_classes : '', :class => "form-control" %>
</div>

<div class="form-bar">
	<%= f.label :page_id %>
	<%= f.select :page_id, Page.where("site_id = ? and id not in (select page_id from list_group_items where id = ?)", @site.id, @list_group_item.list_group.id).order('name asc').collect { |c| [c.name, c.id] }, {prompt: 'Please Select'}, :class => "form-control" %>
</div>